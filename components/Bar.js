import styled from "styled-components";

export const StyledBar = styled.div`
  width: 100%;
  height: 16px;
  margin: 0;
  background-color: var(--main-bg-black);
`;

const Bar = () => <StyledBar></StyledBar>;

export default Bar;
